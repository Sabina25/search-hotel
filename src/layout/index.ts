import styled from 'styled-components';

export const Title = styled.p`
    font-size: 24px;
    font-weight: 600;
    color: white;
    text-align: inherit;
    cursor: pointer;
`;

export const Text = styled.p`
    font-size: 18px;
    font-weight: 400;
    color: white;
    text-align: inherit;
    cursor: pointer;
`;
